pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'nrtsearch-ingestion-plugins'

include 'kafka-plugin'
// Uncomment when ready to implement paimon plugin
// include 'paimon-plugin'